import { LoginData } from "@pages/AuthPage/interfaces";

class AuthService {
  static async login(loginData: LoginData) {

  }

  static async register(loginData: LoginData) {
    
  }
}

export default AuthService;
